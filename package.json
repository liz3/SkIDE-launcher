{
  "name": "skide-fetch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir build && pkg -t node12-macos-x64 -o build/skide_darwin_x64 . && pkg -t node12-linux-x64 -o build/skide_linux_x64 . && pkg -t node12-win-x64 -o build/skide_win_x64 ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "pkg": "^4.4.8",
    "prettier": "^2.0.5"
  }
}
